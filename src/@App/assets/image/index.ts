import noImage from './noimages.png';
import pageNotFound from './page-not-found.png';
import pageNullData from './page-null-data.png';

const images = {
   noImage: noImage,
   pageNotFound: pageNotFound,
   pageNullData: pageNullData,
};

export default images;
